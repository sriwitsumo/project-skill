---
name: thai-project-intake
description: >
  ซักถามผู้ใช้เพื่อเก็บข้อมูลพื้นฐานของโครงงาน/งานวิจัยภาษาไทย (ระดับมัธยมปลายถึงอุดมศึกษา) ให้ครบก่อน
  เริ่มเขียนบทที่ 1, 2, หรือ 3 ของโครงงาน แล้วสรุปเป็น "โปรไฟล์โครงงาน" ไฟล์เดียวที่สกิลเขียนบทอื่น
  (thai-project-chapter1, thai-project-chapter2, thai-project-chapter3) อ่านต่อได้ทันทีโดยไม่ต้องถามซ้ำ
  ใช้สกิลนี้ทุกครั้งที่ผู้ใช้เริ่มต้นโครงงานใหม่ พูดว่า "อยากทำโครงงานเกี่ยวกับ...", "เริ่มทำโครงงาน",
  "ช่วยถามข้อมูลโครงงานหน่อย", "ยังไม่มีข้อมูลโครงงานเลย", หรือขอให้เขียนบทที่ 1/2/3 ทั้งที่ยังไม่เคยให้ข้อมูล
  โครงงานมาก่อนในบทสนทนานี้และไม่มีไฟล์โปรไฟล์โครงงานอยู่แล้ว ถ้ามีไฟล์โปรไฟล์โครงงานอยู่แล้วหรือผู้ใช้ให้
  ข้อมูลครบในข้อความเดียวจนตอบคำถามทุกข้อได้แล้ว ไม่ต้องใช้สกิลนี้ซ้ำ
---

# ตัวช่วยซักข้อมูลโครงงานก่อนเริ่มเขียน

## หน้าที่ของสกิลนี้

สกิลเขียนบทที่ 1/2/3 (`thai-project-chapter1/2/3`) ทุกตัวต้องมีข้อมูลพื้นฐานของโครงงานชุดเดียวกันก่อนเริ่ม
เขียน ถ้าไม่มีกลไกกลาง แต่ละสกิลจะถามคำถามชุดเดิมซ้ำทุกครั้งที่ผู้ใช้สลับไปเขียนคนละบท ทำให้ผู้ใช้ต้องตอบคำถาม
เดิมหลายรอบ สกิลนี้แก้ปัญหานั้นด้วยการ **ซักครั้งเดียวให้ครบ แล้วบันทึกเป็นไฟล์โปรไฟล์โครงงาน** ให้สกิลเขียนบท
อื่นอ่านต่อได้เลย

**ไม่ใช่หน้าที่ของสกิลนี้**: ห้ามเขียนเนื้อหาบทที่ 1/2/3 เอง แค่เก็บข้อมูลดิบแล้วส่งต่อ

## ขั้นตอนการซัก (ถามเป็นชุด ไม่ถามทีเดียวหมดจนผู้ใช้ล้า)

ใช้เครื่องมือถามแบบเลือกตอบ (เช่น AskUserQuestion) กับคำถามที่มีตัวเลือกชัดเจน และถามเป็นข้อความธรรมดา
กับคำถามปลายเปิดที่ต้องพิมพ์อธิบาย **แบ่งเป็น 3 ชุดตามลำดับด้านล่าง อย่าถามข้าม ชุดถัดไปมักขึ้นกับคำตอบของ
ชุดก่อนหน้า**

### ชุดที่ 1: กรอบโครงงาน (ตอบแล้วรู้ทิศทางคำถามชุดถัดไป)

1. **ชื่อเรื่อง/แนวคิดโครงงานคร่าว ๆ** คืออะไร (ถ้ายังไม่มีชื่อทางการ ให้บรรยายสั้น ๆ ว่าทำอะไรก็พอ)
2. **ประเภทโครงงาน** — เลือกจาก: สิ่งประดิษฐ์/นวัตกรรม, ทดลองวิทยาศาสตร์, พัฒนาโปรแกรม/แอป,
   สำรวจ/รวบรวมข้อมูล, สังคมศาสตร์, วิจัยในชั้นเรียน/การศึกษา (ข้อนี้กำหนดว่าต้องถามเรื่องตัวแปร/ประชากรใน
   ชุดที่ 2 หรือไม่)
3. **ระดับการศึกษา/สถาบัน** เช่น มัธยมปลาย ปวช./ปวส. ปริญญาตรี (มีผลต่อความยาวและความเข้มงวดของ
   เนื้อหาที่สกิลเขียนบทจะใช้)

### ชุดที่ 2: เนื้อหาโครงงาน (ปรับคำถามตามประเภทจากชุดที่ 1)

4. **ปัญหาหรือความต้องการตั้งต้น** — สังเกตเจออะไร ที่ไหน เกิดผลกระทบอย่างไร (ให้ผู้ใช้เล่าเป็นข้อความ ยิ่ง
   เจาะจงยิ่งดี เพราะบทที่ 1 และภาษาราชการต้องอ้างคำเดิมนี้ซ้ำตลอดทั้งเล่ม)
5. **สิ่งที่จะลงมือทำ/สร้าง/ทดลอง** เพื่อแก้ปัญหานั้นคร่าว ๆ
6. **ตัวแปรหรือองค์ประกอบหลัก** (ถามเฉพาะเมื่อประเภทเป็นทดลองวิทยาศาสตร์/สิ่งประดิษฐ์ที่มีการทดสอบเชิง
   เปรียบเทียบ) — ตัวแปรต้น ตัวแปรตาม ตัวแปรควบคุม (ถ้ายังตอบไม่ได้ตอนนี้ ข้ามไปได้ สกิลเขียนบทจะช่วยคิด
   ต่อระหว่างเขียนจริง)
7. **กลุ่มเป้าหมาย/ผู้ได้รับผลกระทบ** ของปัญหาและของผลงาน (ถามเฉพาะเมื่อโครงงานเกี่ยวข้องกับคน เช่น
   สำรวจ สังคมศาสตร์ หรือทดสอบระบบกับผู้ใช้จริง)

### ชุดที่ 3: ขอบเขตและข้อกำหนดของสถาบัน

8. **ระยะเวลาที่มี** สำหรับทำโครงงาน (เดือน/สัปดาห์ที่เริ่ม-จบ)
9. **มีแบบฟอร์ม/เทมเพลตของสถาบันหรือไม่** (เช่นไฟล์ .docx/.dotx ที่ครูกำหนด) — ถ้ามี ขอให้แนบไฟล์มาด้วย
   เพราะจะใช้แทนค่าเริ่มต้นของสกิลเขียนบททุกจุด (ลำดับหัวข้อ ความยาวขอบเขต รูปแบบอ้างอิง)
10. **มีไฟล์ข้อมูลจริงประกอบหรือไม่** เช่นผลสำรวจ/แบบสอบถามเป็น Excel/CSV ข้อมูลผลการเรียน — ถ้ามี ให้ระบุ
    ชื่อไฟล์/ตำแหน่งไว้ (สกิลเขียนบทจะเปิดอ่านไฟล์จริงเสมอ ไม่ใช้ตัวเลขที่พิมพ์บอกมาตรง ๆ)

**ถ้าผู้ใช้ตอบข้อมูลมาในข้อความเดียวจนครบทุกข้อแล้ว ให้ข้ามการถามซ้ำ สรุปกลับไปให้ผู้ใช้ยืนยันแทน**

## สร้างไฟล์โปรไฟล์โครงงาน

เมื่อได้ข้อมูลครบ (หรือครบเท่าที่ผู้ใช้ตอบได้ ณ ตอนนี้) ให้บันทึกเป็นไฟล์ Markdown ชื่อ
`โปรไฟล์-[ชื่อโครงงานแบบย่อ].md` ในโฟลเดอร์ทำงานของผู้ใช้ โครงสร้างไฟล์:

```markdown
# โปรไฟล์โครงงาน: [ชื่อเรื่อง]

- ประเภทโครงงาน: [...]
- ระดับการศึกษา/สถาบัน: [...]
- ปัญหา/ความต้องการตั้งต้น: [...]
- สิ่งที่จะลงมือทำ/สร้าง/ทดลอง: [...]
- ตัวแปรต้น/ตาม/ควบคุม: [... หรือ "ยังไม่ระบุ"]
- กลุ่มเป้าหมาย/ผู้ได้รับผลกระทบ: [... หรือ "ไม่เกี่ยวข้อง"]
- ระยะเวลาที่มี: [...]
- แบบฟอร์ม/เทมเพลตสถาบัน: [ชื่อไฟล์แนบ หรือ "ไม่มี ใช้ค่าเริ่มต้นของสกิล"]
- ไฟล์ข้อมูลจริงประกอบ: [ชื่อไฟล์ หรือ "ไม่มี"]

<small>Made by Sriwit Rujirawatanakul</small>
```

ช่องไหนที่ผู้ใช้ยังตอบไม่ได้ ให้ใส่ `[ยังไม่ระบุ]` แทนการเดา — ห้ามเติมข้อมูลที่ผู้ใช้ไม่ได้ให้มาโดยเด็ดขาด (หลัก
การเดียวกับ "ห้ามคิดข้อมูลขึ้นเอง" ในสกิลเขียนบท)

## ส่งต่อให้สกิลเขียนบท

หลังบันทึกไฟล์โปรไฟล์เสร็จ ให้ถามผู้ใช้ว่าต้องการให้เริ่มเขียนบทไหนต่อ (บทที่ 1, 2, หรือ 3) แล้วเรียกใช้สกิล
`thai-project-chapter1`, `thai-project-chapter2`, หรือ `thai-project-chapter3` ตามที่เลือก โดยส่งต่อเนื้อหาใน
ไฟล์โปรไฟล์เป็นบริบทให้สกิลนั้นใช้ทันที **ไม่ต้องให้สกิลเขียนบทถามคำถามชุดเดิมซ้ำอีก** เว้นแต่ข้อมูลในโปรไฟล์
มีช่องที่ยังเป็น `[ยังไม่ระบุ]` และจำเป็นต้องใช้กับบทที่กำลังจะเขียนจริง ๆ

<small>Made by Sriwit Rujirawatanakul</small>
